---
description: Inspect React component and fix all UI differences to match Foundit.in exactly
---

Fix the UI to exactly match Foundit.in for: **$ARGUMENTS**

Process:
1. Read the React component file for this page/section
2. Compare its structure against Foundit.in design — spacing, colors, fonts, layout, missing elements
3. List every visual difference found in the code
4. Fix each difference directly in the React component
5. Verify all Foundit.in brand values are applied correctly

Brand values to enforce:
- Primary Orange: #f04e23 (all CTAs and highlights)
- Dark Navy: #1a1a2e (headers and nav)
- Text Dark: #333333, Text Muted: #666666
- Border: #e0e0e0, Background: #f5f5f5
- Card shadow: 0 2px 8px rgba(0,0,0,0.1), border-radius cards: 4px, buttons: 2px
- Font: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif
