---
description: Update Supabase database schema using Supabase MCP
---

Using the **Supabase MCP**, perform this database operation:

**$ARGUMENTS**

Rules:
- Use Supabase MCP tools directly
- Maintain Row Level Security (RLS) on all tables
- Add indexes for any new searchable or filterable columns
- Update the schema section in CLAUDE.md if tables or columns change
- Confirm the operation succeeded before reporting done
