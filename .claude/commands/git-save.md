---
description: Stage, commit and push all changes to GitHub
---

Save all current work to GitHub:

1. Run: git add -A
2. Run: git commit -m "feat: $ARGUMENTS"
3. Run: git push origin main

If push fails because remote does not exist, use GitHub MCP to create the repo first then push.
Write a clear commit message describing what was completed.
